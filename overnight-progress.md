- Implemented and verified the first Strategy League persistence/API slice: schema-versioned, atomic local storage with a 50-match cap; validated compact Red/Blue completed-match summaries; independent revision-0 Red/Blue profiles marked `pending-provider`; aggregate wins and newest-first recent results.
- Added same-origin companion endpoints: `GET /api/league` and record-only `POST /api/league/matches`. Browser-facing endpoints cannot mutate strategy profiles, preserving the OpenAI/Codex review boundary.
- Verified: `node tools/league-store-check.mjs` and `node tools/league-api-check.mjs` passed. Existing commander runtime/provider checks passed.
- Blocker verified: existing `tools/model-proxy-check.mjs` against port 8812 found a pre-existing enabled commander server (`{"enabled":true,"lmStudio":"pending"}`), so its expected disabled-state check failed. No provider call was made and this run did not change that server.
- Implemented Watch completion telemetry: every ended Watch match posts one validated Red/Blue summary to the same-origin companion service, with duration, team gold/spend, living composition, cumulative unit losses, and the selected strategy revisions. Deterministic mechanics now retain team loss totals.
- Added team-scoped, read-only `GET /api/league/strategy/red|blue`; Model Commander loads each team’s own sanitized profile only, and commander state excludes the opponent profile. Missing or unreachable companion/profile remains an explicit no-profile boundary; no provider call/review was fabricated.
- Verified: `node tools/watch-league-check.mjs`, `node tools/league-store-check.mjs`, `node tools/league-api-check.mjs`, `node tools/commander-runtime-check.mjs`, and `node tools/commander-provider-check.mjs` passed.
- Blockers verified: `node tools/model-commander-ai-check.mjs` fails its pre-existing capped-turret expectation because the world now starts with a turret and the test adds only two more, so queued `structure` is legal rather than its expected `warrior`. Browser check could not run because Chrome CDP at `127.0.0.1:9223` refused connection; port 8034 was already occupied, so no server/process was changed.
- Implemented the main-menu Strategy League history screen. It fetches only the same-origin aggregate companion endpoint, displays total matches/wins, five recent results, and Red/Blue profile revision/review status; it has explicit loading and unavailable states and does not provide strategy mutation or model context.
- Verified: `node --check src/main.js`, `node --check src/render/ui.js`, `node tools/league-history-view-check.mjs`, `node tools/league-api-check.mjs`, `node tools/watch-league-check.mjs`, `node tools/league-store-check.mjs`, and `git diff --check` passed.
- Added a trusted offline review-application boundary: `tools/strategy-league-review.mjs --input reviewed-profiles.json [--data league.json]` atomically advances both Red/Blue profiles to reviewed revisions after an externally completed OpenAI/Codex review. It makes no provider request and remains outside all browser APIs; malformed/incomplete reviews are rejected.
- Verified: updated `node tools/league-store-check.mjs`, `node tools/league-api-check.mjs`, `node tools/watch-league-check.mjs`, `node tools/commander-runtime-check.mjs`, `node tools/commander-provider-check.mjs`, syntax checks, `git diff --check`, and an isolated trusted-review CLI persistence invocation all passed. No provider call or review result was fabricated.
- Hardened companion persistence against concurrent local API writes: match recordings and trusted review applications now serialize their read-modify-write cycle, so atomic rename cannot be undermined by a lost update. `snapshot()` waits for queued writes. Added a simultaneous 12-match regression case verifying newest-first bounded history retains every queued completion.
- Verified: `node --check src/strategy/leagueStore.js`, `node --check tools/league-store-check.mjs`, `node tools/league-store-check.mjs`, `node tools/league-api-check.mjs`, `node tools/watch-league-check.mjs`, `node tools/league-history-view-check.mjs`, `node tools/commander-runtime-check.mjs`, `node tools/commander-provider-check.mjs`, and `git diff --check` passed. No provider call was made.
- Hardened the Local Gemma provider boundary: companion `/api/commander` now accepts only `player`/`ai`, replaces any browser-supplied profile with the matching persisted Red/Blue profile, and reduces observations to fixed bounded numeric/composition fields before prompting. This keeps opponent profiles and arbitrary browser text out of model context; the deterministic browser simulation still owns mechanics and legality.
- Verified: `node tools/commander-context-check.mjs`, commander/runtime/provider, Watch-league, store, and API checks; `node tools/headless.js`; syntax checks; and `git diff --check` all passed. No provider request was made.
- Hardened the persisted league-data boundary: malformed stored matches are discarded before aggregate/history API projection, and invalid stored profiles reset safely per team rather than becoming model context. Valid persisted profile summaries are normalized and team identity remains server-owned.
- Verified: expanded `node tools/league-store-check.mjs` corruption regression plus `node tools/league-api-check.mjs`, `node tools/watch-league-check.mjs`, `node tools/commander-context-check.mjs`, commander runtime/provider checks, `node tools/league-history-view-check.mjs`, `node tools/headless.js`, `node --check src/strategy/leagueStore.js`, and `git diff --check` passed. No provider request was made.
- Added the missing trusted review-input boundary: `tools/strategy-league-review-packet.mjs [--data league.json]` emits a bounded, newest-first compact match-history packet with aggregate wins and the current independent Red/Blue profiles. It is offline-only, exposes no HTTP endpoint, and makes no provider request; its output can be externally reviewed before application through the existing trusted review CLI.
- Verified: expanded store packet/corruption regression; `node tools/strategy-league-review-packet-check.mjs`; league API, Watch telemetry, bounded commander-context, commander runtime/provider, and history-view checks; `node tools/headless.js`; syntax checks; and `git diff --check` all passed. No provider credential or OpenAI/Codex review result was available or fabricated.
- Hardened the trusted OpenAI/Codex review boundary against stale external output: review packets now expose the independent Red/Blue revisions reviewed, and offline profile application requires those exact `basedOnRevisions` before advancing either profile. A completed older review now fails safely instead of overwriting a newer profile; this remains outside browser APIs and makes no provider call.
- Verified: expanded store regression rejects stale review revisions; review-packet check exercises packet-to-offline-CLI profile application; league API, Watch telemetry, bounded commander context, runtime/provider, history view, headless invariants, syntax checks, and `git diff --check` passed. No provider request or review result was fabricated.
- Hardened asynchronous Local Gemma command application: a companion response now applies only to the exact still-playing Watch world and controller that requested it. Late responses after Back/rematch or match completion are discarded and cannot steer another deterministic match.
- Verified: expanded `node tools/commander-runtime-check.mjs` stale/finished-response regression, commander context/provider, Watch telemetry, league store/API/review-packet, history-view, `node tools/headless.js`, syntax checks, and `git diff --check` passed. No provider request was made.
- Hardened the Local Gemma output boundary: commander decisions are now normalized again immediately before deterministic Watch state application, so malformed companion responses cannot set invalid commands or inject duplicate/unbounded purchase priorities. Valid priorities are allowlisted, unique, and capped at four.
- Verified: expanded commander provider/runtime regressions plus commander-context, league store/API, Watch telemetry, history, trusted review-packet, headless invariants, syntax checks, and `git diff --check` passed. No provider request was made.
- Hardened the bounded companion match-history boundary: completed-match duration, economy, losses, and every composition count now have explicit finite upper limits in addition to the existing 50-record cap. Oversized browser telemetry is rejected before persistence, aggregates, review packets, or Local Gemma-adjacent context can consume it.
- Verified: expanded `node tools/league-store-check.mjs` rejects oversized duration, gold, losses, and composition; league API, Watch telemetry, history view, trusted review-packet, commander context/runtime/provider, `node tools/headless.js`, syntax checks, and `git diff --check` passed. No provider request was made.
- Hardened the same-origin companion request boundary: JSON request bodies now have a true 16 KiB byte limit (including an early `Content-Length` rejection) and are drained safely after rejection, preventing unbounded body accumulation while preserving normal API errors. Added API regression coverage that an oversized match request is rejected and cannot mutate persisted league history.
- Verified: `node tools/league-api-check.mjs`, league store, Watch telemetry, trusted review-packet, commander context/runtime/provider, and `node tools/headless.js` passed; syntax checks and `git diff --check` passed. `node tools/release-gate.mjs` reached browser verification but failed its pre-existing desktop Watch-speed expectation (`1,1,1,1,1` versus expected `1,5,10,20,1`); no provider request was made.
- Repaired the focused desktop Watch-speed regression test to click the relocated bottom spectator control (while retaining the separate interactive-mode zoom assertion). This restores real browser/release coverage for the Watch control layout without changing simulation, companion APIs, or provider behavior.
- Verified: `node tools/release-gate.mjs` passed all syntax, deterministic simulation, desktop Watch speed/camera, mobile pan, and mobile orientation/pinch checks. League store/API, Watch telemetry, bounded commander context/runtime/provider, history view, trusted review-packet checks, and `git diff --check` also passed. No provider request was made.
- Hardened bounded Strategy League persistence further: browser-reported selected strategy revisions and hand-edited persisted profile revisions now have a 1,000,000 ceiling; trusted review application fails explicitly at that ceiling rather than retaining unbounded numeric telemetry/profile state. Added store regressions for oversized match revisions and persisted profile revisions.
- Verified: `node --check src/strategy/leagueStore.js`, expanded `node tools/league-store-check.mjs`, league API/Watch telemetry/commander/history/review-packet checks, `node tools/headless.js`, `git diff --check`, and full `node tools/release-gate.mjs` passed. No provider request or external review result was made or fabricated.
- Hardened the browser-side own-strategy loading boundary: same-origin profile responses now require bounded revisions, nonblank bounded summaries, and valid bounded timestamps before they can enter Local Gemma context or completion telemetry. Malformed profiles resolve to the existing explicit no-profile boundary; no browser path can mutate review state.
- Verified: expanded `node tools/watch-league-check.mjs` rejects oversized revisions/summaries, blank summaries, and malformed timestamps; commander runtime/context/provider, league store/API/history/review-packet, deterministic headless invariants, `git diff --check`, and full browser `node tools/release-gate.mjs` all passed. No provider request or external OpenAI/Codex review was made.
- Hardened the companion same-origin boundary: API requests with an `Origin` header must match the serving game origin; cross-origin requests are rejected before League recording or Model Commander routing, with bodies drained safely. Offline no-Origin tool access remains supported and no CORS surface was added.
- Verified: expanded `node tools/league-api-check.mjs` rejects a cross-origin match write without history mutation and accepts a same-origin write; league store, Watch telemetry, commander context/runtime/provider, history, review-packet, deterministic headless invariants, `git diff --check`, and full browser `node tools/release-gate.mjs` passed. No provider request or external OpenAI/Codex review was made.
- Hardened strategy-profile review-state invariants: `pending-provider` now accepts only the canonical revision-0/no-timestamp sentinel, while `reviewed` profiles require a positive revision and valid timestamp. Hand-edited or malformed companion/storage profiles therefore reset to the explicit no-profile boundary rather than entering Local Gemma context as unreviewed text.
- Verified: expanded league store and Watch profile-boundary regressions; `node --check` for strategy modules; league API, trusted review-packet, commander context/runtime/provider, and `git diff --check` all passed. No provider request or external review was made.
- Added idempotent Watch completion persistence: browser telemetry now assigns an opaque per-completed-world match id outside deterministic simulation; the companion retains a single history record when that completion is retried. Existing v1 records without ids remain readable, and malformed ids are rejected before persistence.
- Verified: expanded store, same-origin API, and Watch telemetry regressions; commander context/runtime/provider checks; deterministic `node tools/headless.js`; `git diff --check`; and full browser `node tools/release-gate.mjs` all passed. No provider request or external OpenAI/Codex review was made.
- Hardened the trusted OpenAI/Codex review boundary against evidence staleness: review packets now include a SHA-256 digest of their exact bounded match history, and offline profile application requires that digest alongside profile revisions. A match recorded during external review now rejects the stale review rather than applying summaries based on outdated evidence; this remains offline-only and outside browser APIs.
- Verified: expanded `node tools/league-store-check.mjs` regression rejects stale-history reviews; `node tools/strategy-league-review-packet-check.mjs`, same-origin league API, Watch telemetry, bounded commander context/runtime/provider, history view, deterministic headless invariants, `git diff --check`, and full browser `node tools/release-gate.mjs` passed. No provider request or external OpenAI/Codex review was made.
- Hardened idempotent Watch completion persistence: a reused opaque match id now deduplicates only an identical normalized completion. A conflicting winner/economy/composition/revision payload is rejected with HTTP 409 and cannot silently suppress or mutate match history.
- Verified: expanded store and same-origin API regressions for conflicting id reuse; Watch telemetry, trusted review-packet, league history, bounded commander context/runtime/provider checks, deterministic `node tools/headless.js`, `git diff --check`, and full browser `node tools/release-gate.mjs` passed. No provider request or external OpenAI/Codex review was made.
- Hardened companion write boundaries: browser-facing match telemetry and Model Commander requests now require `application/json` (including charset variants), safely drain rejected bodies, and reject HTML-form/non-JSON posts before parsing, persistence, or model routing. This complements same-origin enforcement without adding CORS or changing deterministic mechanics.
- Verified: expanded `node tools/league-api-check.mjs` rejects a form-encoded write with HTTP 415 and no history mutation; syntax checks, league store, Watch telemetry, bounded commander context/runtime/provider, history, review-packet, deterministic `node tools/headless.js`, `git diff --check`, and full browser `node tools/release-gate.mjs` passed. `node tools/model-proxy-check.mjs` still finds the pre-existing opt-in commander server on port 8812 (`enabled:true`, `lmStudio:pending`) and therefore fails its disabled-server expectation; no provider request was made and this run did not change that server.
- Repaired Local Gemma command application so every validated commander command now crosses the deterministic `setTeamCommand` boundary. Existing units immediately receive attack/defend/retreat orders, and retreat clears their targets; previously only team metadata changed, leaving active units on stale commands.
- Verified: expanded `node tools/commander-runtime-check.mjs` covers unit-order propagation and retreat target clearing; commander provider/context, Watch telemetry, league store/API/history/review-packet checks, deterministic `node tools/headless.js`, `git diff --check`, and full browser `node tools/release-gate.mjs` passed. No provider request or external OpenAI/Codex review was made.
- Hardened the explicit pending-provider boundary: a JSON Model Commander request now drains safely before returning 503 when Local Gemma is disabled, so an unavailable provider cannot leave a keep-alive request body unread. Added same-origin API coverage for this no-provider response.
- Verified: `node tools/league-api-check.mjs`, commander context/runtime/provider, league store, Watch telemetry/history/review-packet checks, `node tools/headless.js`, `git diff --check`, and full browser `node tools/release-gate.mjs` passed. No provider request or external OpenAI/Codex review was made.
- Hardened the companion read-only boundary: `/api/commander/status` now accepts `GET` only and drains rejected request bodies before returning 405, preventing an accidental mutation/body-processing surface around provider availability.
- Verified: expanded `node tools/league-api-check.mjs` covers rejected `POST /api/commander/status`; syntax checks, all league/Watch/commander focused checks, deterministic `node tools/headless.js`, `git diff --check`, and full browser `node tools/release-gate.mjs` passed. No provider request or external OpenAI/Codex review was made.
- Fixed a bounded-companion request-body edge case: chunked JSON telemetry exceeding 16 KiB now continues draining without retaining post-limit chunks in memory. Added streaming API regression coverage for an oversized no-`Content-Length` body and verified it is rejected without history mutation.
- Verified: `node --check tools/lan-preview-server.mjs`, `node --check tools/league-api-check.mjs`, `node tools/league-api-check.mjs`, league store/Watch/commander/history/review-packet checks, deterministic `node tools/headless.js`, `git diff --check`, and full browser `node tools/release-gate.mjs` passed. No provider request or external OpenAI/Codex review was made.
- Ad-hoc verification: an isolated temporary `hermes-verify-*.mjs` script issued a multi-chunk JSON body over 16 KiB without `Content-Length` to the companion and confirmed HTTP 400 plus zero persisted matches; the temporary script/data and its preview process were removed afterward.
- Hardened completed Watch telemetry delivery: browser persistence now makes up to three bounded retries for transient network/5xx failures using the existing opaque match id, while 4xx/409 responses remain non-retryable. This preserves deterministic simulation ownership and lets companion idempotency safely cover a write whose response was lost.
- Verified: expanded `node tools/watch-league-check.mjs` exercises same-id transient retry and non-retryable conflict behavior; `node --check` changed files, league store/API, commander runtime/context/provider, history/review-packet, `node tools/headless.js`, `git diff --check`, and full browser `node tools/release-gate.mjs` all passed. No provider request or external OpenAI/Codex review was made.
- Hardened all companion API method boundaries: rejected non-GET/non-POST requests now drain any body before returning 405, including read-only League/profile routes. This prevents unexpected request bytes persisting on a keep-alive connection and does not add mutation, CORS, or provider behavior.
- Verified: expanded `node tools/league-api-check.mjs` sends bodies to rejected read-only mutations and confirms no history change; `node --check` for changed tools, league store/Watch telemetry/commander context-runtime-provider checks, `git diff --check`, and full browser `node tools/release-gate.mjs` passed. No provider request or external OpenAI/Codex review was made.
- Ad-hoc verification: a temporary `hermes-verify-*.mjs` script used one keep-alive connection to send bodies to rejected League/profile/match/commander methods, then read `/api/league` after each. It confirmed each received 405, follow-up reads remained usable, and no match was recorded; the temporary script/data and preview process were removed afterward.
- Hardened local Strategy League storage recovery: syntactically invalid/interrupted `league.json` now fails closed to the explicit empty history/pending-provider profiles boundary rather than breaking companion readers. The next valid completed Watch summary atomically restores a fresh bounded history; no browser mutation or provider path was added.
- Verified: expanded store and same-origin API regressions cover malformed JSON read/recovery; league API, Watch telemetry, trusted review-packet, commander context/runtime/provider, history-view, deterministic headless invariants, syntax checks, `git diff --check`, and full browser `node tools/release-gate.mjs` all passed. No provider request or external OpenAI/Codex review was made.
- Hardened persisted Watch completion idempotency: duplicated opaque match ids found in a hand-edited/interrupted `league.json` are now discarded during sanitization, preserving only the earliest valid completion. Duplicate storage entries can no longer inflate aggregate wins, history UI, or the trusted review packet.
- Verified: expanded `node tools/league-store-check.mjs` with a conflicting persisted duplicate-id regression; syntax checks, same-origin league API, Watch telemetry, trusted review-packet, bounded commander context/runtime/provider, history view, deterministic `node tools/headless.js`, `git diff --check`, and full browser `node tools/release-gate.mjs` all passed. `OPENAI_API_KEY`, Codex, and LM Studio model environment variables were unavailable; no provider request or external review result was made or fabricated.

- Hardened persistent Strategy League writes across separate local Node processes: the companion and trusted offline review CLI now hold a bounded exclusive sidecar lock around each read-modify-write transaction, preventing either atomic rename from losing the other writer's completed match or profile update.
- Verified: expanded `node tools/league-store-check.mjs` with simultaneous writes through two independent store instances; store/API/Watch telemetry/review-packet, commander context/runtime/provider, history view, deterministic headless invariants, `git diff --check`, and full browser `node tools/release-gate.mjs` passed. No provider request or external review result was made.
- Hardened local Strategy League write recovery: a sidecar lock left by a crashed companion or offline review CLI is reclaimed after one minute, so it cannot permanently block subsequent bounded Watch-history writes or trusted profile application.
- Verified: expanded `node tools/league-store-check.mjs` creates an aged interrupted lock and confirms the next completion persists; league API, Watch telemetry, trusted review packet, commander context/runtime/provider checks, `git diff --check`, and full browser `node tools/release-gate.mjs` passed. No provider request or external OpenAI/Codex review was made.
- Added a true multi-process persistence regression: eight concurrent child Node processes now record independent completed Watch summaries against one league file, verifying the sidecar lock preserves every bounded-history write across the companion/offline-CLI process boundary (not merely separate in-memory stores).
- Verified: `node --check` for the regression/worker, expanded `node tools/league-store-check.mjs`, league API, Watch telemetry, trusted review-packet, bounded commander context/runtime/provider, history view, deterministic headless invariants, `git diff --check`, and full browser `node tools/release-gate.mjs` passed. `OPENAI_API_KEY` and `LM_STUDIO_MODEL` were unavailable; no provider request or external OpenAI/Codex review result was made or fabricated.
- Fixed the real Windows multi-process burst revealed by ad-hoc verification: sidecar lock acquisition now remains bounded but waits up to 10 seconds (previously 2), allowing concurrent companion/offline-CLI startup writes to serialize instead of spuriously failing busy.
- Verified: focused temporary `hermes-verify-league-multiprocess.mjs` under the OS temp directory launched eight independent worker processes and confirmed all eight distinct completions persisted; it and its temporary data were removed. Syntax checks, expanded league-store regression, and `git diff --check` passed.

- Hardened the same-origin companion’s remaining static-file method boundary: unsupported methods with a request body now drain before HTTP 405, so a rejected same-origin static POST cannot leave bytes queued on a reused connection. Added an API regression that uses one keep-alive socket for the rejected static POST and a subsequent League read, confirming the connection remains usable and history unchanged.
- Verified: `node --check tools/lan-preview-server.mjs`, `node --check tools/league-api-check.mjs`, `node tools/league-api-check.mjs`, league store/Watch telemetry/commander context-runtime-provider/history/review-packet checks, deterministic `node tools/headless.js`, and `git diff --check` passed. `OPENAI_API_KEY`, `LM_STUDIO_MODEL`, and `LM_STUDIO_URL` were unavailable; the explicit pending-provider boundary remains in effect and no provider request or external review result was made or fabricated.
- Hardened the Local Gemma companion boundary against stalled enabled providers: every LM Studio inference now uses a validated bounded timeout and abort signal, so a hung local model fails through the existing unavailable-provider error path rather than holding a same-origin command request indefinitely. This adds no scripted fallback and preserves deterministic command validation.
- Verified: expanded `node tools/commander-provider-check.mjs` proves a stalled fetch aborts and invalid timeout settings are rejected; commander context/runtime, league store/API, Watch telemetry, trusted review-packet, history view, deterministic headless invariants, `git diff --check`, and full browser `node tools/release-gate.mjs` passed. `OPENAI_API_KEY`, `LM_STUDIO_MODEL`, and `LM_STUDIO_URL` remain unavailable; no provider request or external OpenAI/Codex review result was made or fabricated.

- Hardened the Local Gemma provider boundary against oversized inference payloads: successful LM Studio responses are streamed and capped at 16 KiB before JSON parsing, with an early `Content-Length` rejection. Oversized output now fails through the existing unavailable-provider path without unbounded companion memory use; no strategic fallback was added.
- Verified: expanded `node tools/commander-provider-check.mjs` covers valid streamed JSON plus oversized streamed response rejection; commander context/runtime, league store/API, Watch telemetry, history/review-packet, deterministic `node tools/headless.js`, `git diff --check`, and full browser `node tools/release-gate.mjs` passed. `OPENAI_API_KEY`, `LM_STUDIO_MODEL`, and `LM_STUDIO_URL` remain unavailable; no provider request or external OpenAI/Codex review result was made or fabricated.
- Hardened the final same-origin companion static-file boundary: percent-encoded URL traversal is decoded before path resolution and rejected with a path-aware containment check, preventing sibling/local file access through static routing.
- Verified: expanded `node tools/league-api-check.mjs` rejects encoded static traversal; syntax checks, league store/Watch telemetry/commander context-runtime-provider/history/review-packet checks, deterministic `node tools/headless.js`, `git diff --check`, and full browser `node tools/release-gate.mjs` passed. `OPENAI_API_KEY`, `LM_STUDIO_MODEL`, and `LM_STUDIO_URL` are unavailable, so the explicit pending-provider boundary remains; no provider request or external OpenAI/Codex review result was made or fabricated.
- Added real browser coverage for the main-menu Strategy League screen. The release gate now starts the same-origin companion with an isolated temporary bounded league fixture; a new CDP check clicks Strategy League and verifies aggregate wins, newest-first results, and independent reviewed/pending profile status load through the actual companion API. The fixture is removed after the gate and does not touch persistent league data.
- Verified: `node tools/league-store-check.mjs`, `node tools/league-api-check.mjs`, `node tools/watch-league-check.mjs`, `node tools/league-history-view-check.mjs`, commander context/runtime/provider checks, trusted review-packet check, deterministic `node tools/headless.js`, `git diff --check`, and full `node tools/release-gate.mjs` passed (including the new Strategy League browser check). Direct standalone browser-check invocation was blocked because no Chrome CDP listener was running on port 9223; the release gate launched its own CDP browser and passed. No provider request or external OpenAI/Codex review was made.
